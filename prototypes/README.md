# Design Prototype v0

The supplied taejung-portfolio.html is retained unchanged as the content and design reference. It is not included in the published assets.

The existing Astro source and FOWOCO detail remain intact. The build first renders the existing Astro detail and then builds the React + Vite home over the root entry. The other project routes use a shared React placeholder. This preserves the existing detail without expanding its scope.
