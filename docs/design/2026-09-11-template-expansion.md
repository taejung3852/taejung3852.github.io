# Portfolio template expansion

The user approved FOWOCO as the first design baseline. Preserve its illustration and layout. Extend the same five-scene structure, viewport rhythm, monochrome palette, snap behavior and integrated footer to HWPX, OwnHands and LLM Gateway.

Home: header + Hero share the first snap destination; About and contribution introduction have individual destinations. The six existing contribution scenes retain their horizontal transitions and receive native vertical snap anchors. Each project introduction is a separate scene with a working detail link. Footer belongs to the final project scene.

The new pages are narrative skeletons. Current fixed-revision READMEs are reviewed, not a substitute for code, personal attribution, historic PR or runtime verification. Use explicit pending labels for implementation claims. OwnHands README currently describes DevHarness M1; do not imply a finished production UI. Gateway retrospective REST/SSE is not current implementation. No fabricated metrics or performance charts. Domain illustrations are explanatory UI diagrams, not product screenshots.

Check desktop 1920x1080, 1440x900, 1366x768, 1280x720, 2560x1080, 1024x768, tablet 768x1024 and mobile 390x844. Confirm routes, theme, snap endpoints, contribution scene transitions, footer visibility and reduced motion. Save only; no production deployment.

## Validation completed

- Build passed; five routes inspected at eight viewport sizes (40 combinations). No page overflow or desktop scene-height failures.
- Home contribution slides 0–5 and back: active slide matched every snap anchor. About wheel snap aligned to zero.
- Home/detail/back-anchor navigation, home footer endpoint, and light/dark switching passed.
- Reduced-motion layout checked in viewport matrix; animated detail scene and home contribution transitions checked separately.
- Current FOWOCO component and study CSS remain unchanged.
- Fixed README refs: HWPX 16e3a3985831fd67fa6290223ab405f7e4261fb3; OwnHands 779b5568c38a154b4dc7b55818e806a5e53639f0; Gateway fc82bbce0e04934be88684fe2fd400271998a346. These are narrative sources, not personal attribution or runtime proof.
