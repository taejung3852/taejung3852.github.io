import React from 'react';
import {Link} from 'react-router-dom';
import {projects,projectHighlights,projectSlugs} from '../content/site';
function HighlightedSummary({project}){const phrase=projectHighlights[project.name];const index=phrase?project.summary.indexOf(phrase):-1;if(index<0)return project.summary;return <>{project.summary.slice(0,index)}<strong className="project-emphasis">{phrase}</strong>{project.summary.slice(index+phrase.length)}</>}
export default function ProjectList({sectionNumber='02'}){return <section className="projects shell" id="projects" aria-labelledby="projects-title">{projects.map((p,i)=><div className="home-project-scene" key={p.name}>{i===0&&<div className="projects-heading"><div className="section-heading"><span className="section-index">{sectionNumber} /</span><h2 id="projects-title">주요 프로젝트</h2></div><p className="section-intro">어떤 문제를 만났고, 왜 그렇게 설계했는지.<br/>구현과 검증의 근거를 함께 담았습니다.</p></div>}<article className="project-row"><div className="project-title"><span className="project-number">{String(i+1).padStart(2,'0')}</span><div className="project-identity"><h3><Link to={`/projects/${projectSlugs[i]}`}>{p.name}</Link></h3><Link className="project-image-guide" to={`/projects/${projectSlugs[i]}`} aria-label={`${p.name} 프로젝트 페이지 보기`}><span>대표 이미지</span></Link></div></div><div className="project-details"><p className="project-summary"><HighlightedSummary project={p}/></p><p className="project-role">{p.role}</p><p className="project-status">{p.status}</p><Link className="project-detail-link" to={`/projects/${projectSlugs[i]}`}>프로젝트 보기 <span aria-hidden="true">→</span></Link></div><a className="repository-link" href={p.href} target="_blank" rel="noreferrer" aria-label={`${p.name} 저장소 보기, 새 탭`}><span>{p.linkLabel}</span><span className="link-arrow" aria-hidden="true">↗</span></a></article>{i===projects.length-1&&<footer className="home-inline-footer"><Link to="/">박태정</Link><a href="/documents/taejung-resume.pdf" download>이력서 다운로드 ↗</a></footer>}</div>)}</section>}
